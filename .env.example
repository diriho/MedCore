# HTTPS URL where the web app is reachable (for Health ID QR on another phone). Example: https://abc.trycloudflare.com
VITE_PUBLIC_APP_ORIGIN=

# Backend (API server) environment vars live in server/.env.example
